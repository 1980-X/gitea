---
description: 本仓 Agent 长期记忆 — Karpathy wiki + agentmemory（跨会话）
alwaysApply: true
---

# Agent 记忆（本仓）

1. **领域/运维知识**：先查 `wiki/index.md`；按需用 skill `karpathy-llm-wiki` 做 ingest / query / lint。真源仍是 compose、`.env.example`、skill `gitea-ops`。
2. **跨会话回想**：MCP `agentmemory`（本机 `:3111`）。会话开始或涉及既往决策时 recall；重要结论 remember。勿写入 `.env` 密码。
3. **禁止**依赖 SoulVoix / Helix / 其它业务仓的 wiki 当真源；不上 Mem0 云 / Graphiti / Letta。
4. 说明见 `.cursor/docs/agent-memory.md`。
